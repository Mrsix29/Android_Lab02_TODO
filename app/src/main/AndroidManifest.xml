<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.nait.zli.lab02todoornot">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab02TodoOrNot">
        <activity android:name=".ArchivedViewActivity"/>
        <activity android:name=".PrefsActivity" />
        <activity
            android:name=".EditContentActivity"
            android:label="@string/title_activity_edit_content"
            android:theme="@style/Theme.Lab02TodoOrNot.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>